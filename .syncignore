# Files excluded from sync to raycast-ext-github-trim/extensions/cmux/
# (PR #26595 — Add cmux extension to raycast/extensions)
#
# Each line is a path relative to the raycast_cmux repo root.
# Lines starting with # are comments. Blank lines are ignored.

# Auto-generated by Raycast CLI; the upstream extensions monorepo has its own
# convention for this file and does not track per-extension copies.
raycast-env.d.ts

# Local-only development docs, not for upstream review.
# raycast_cmux tracks COMPASS/ as a standalone repo, but the contents are
# personal project notes that should not appear in the Raycast Store PR.
COMPASS/
